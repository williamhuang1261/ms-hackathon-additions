# Vercel AI SDK / Google Generative AI (Gemini) key for /api/thanks.
# Get a free key at https://aistudio.google.com/app/apikey
# Without this set, /api/thanks serves a deterministic fallback message
# instead of a real model call.
GOOGLE_GENERATIVE_AI_API_KEY=
